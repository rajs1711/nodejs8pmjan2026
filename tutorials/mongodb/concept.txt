What is MongoDB?

MongoDB is a NoSQL, document-oriented database designed to store and manage large amounts of 
unstructured or semi-structured data.

Instead of storing data in rows and tables like traditional relational databases,
 MongoDB stores data in JSON-like documents called BSON (Binary JSON).

Example:

{
  "name": "Rahul",
  "age": 22,
  "course": "BCA",
  "address":{
    "permanent":"patna",
    "temporary":"delhi"
  },
  "college":{
    "name":"St.Marry",
    "address:"patna",
    "type":"private"
  }
}


Features of MongoDB:

1. Document-Oriented Storage
Data is stored as documents instead of tables.
Documents are easy to read and modify.
Similar to JSON format.

{
  "product": "Laptop",
  "price": 50000
}

2. Schema-less Database
No fixed structure is required.
Different documents in the same collection can have different fields.

{ "name": "Amit" }

{ "name": "Priya", "age": 20 }

3. Scalability

MongoDB supports horizontal scaling using sharding.

This means:

Data can be distributed across multiple servers.
Helps handle very large datasets and traffic.

4. Indexing Support

MongoDB supports indexes for faster searching.

Types include:

Single field index
Compound index
Text index
Geospatial index

5. Aggregation Framework

MongoDB provides powerful data processing and analytics tools.

Used for:

Filtering
Grouping
Sorting
Calculations

6. Cloud Support

MongoDB provides cloud database services through:

MongoDB Atlas

Features:

Automated backups
Monitoring
Global deployment
Security tool


===============
comparison
===============
SQL => database => table => rows/columns 

mongodb => datatbase => collections => document 

================================================
MongoDB Cheat Sheet:



1. show dbs;  => to display all databases 
2. use <dbnam> =>
     use nodejs830pm ; => helps to switch from one database
                          to other or if the db is available it will create new db and switch to that db .


Note: To show yoor new  database  in DB list =>your database have at least one collection .

3. Create Collection :
    db. createCollection('students');

4. Data Insert under collection 
   A. Single Insert
=================INSERT ======================
    db.students.insert({
        Name:"Raj",
        Mobile:82102023331,
        Email:abbsv@gmail.com
    });

   B. Multiple insert  

    db.students.insertMany(
        [
        {
        Name:"Amit",
        Mobile:82102023331,
        Email:abbsv@gmail.com
        },
        {
        Name:"Summet",
        Mobile:82102023331,
        Email:abbsv@gmail.com
        }
        ]
    );   

    Note: whenever a document inserted in your collection , mongodb assign a unique _id to every document .
==============SELECT ALL DATA=============================

5. To list (show) all data in collection:
   sql => select * from students ;
   Mongodb=>db.students.find();

===========To SELECT PARTICULAR FILED===============
6. To List all data in collection but we want only name and email .
sql => select name ,email from users ;
mongodb => db.users.find({},{name:1,email:1})

note: mongodb return _id key by default if you now want this _id key in your response then forecfully set it to 0;

===========To HIDE _ID field=====================
db.users.find({},{name:1,email:1,_id:0})

========Implement where Condition========
7. WAQ to find user details where name is saurav .
sql => select * from users where name='saurav';
mongodb => db.users.find({name:'saurav'})

8. WAQ to find user email and mobile whose name is saurav .

sql => select email , mobile from users where name='saurav'
mongodb => db.users.find({name:'saurav'},{email:1,mobile:1,_id:0})

=======Implemet Not Equals to operator =============
9. wAQ to find users details whose mobile not equal to 8210202331 .

sql=>select * from users where mobile !='8210202331'

mongodb =>db.users.find({
  mobile:{$ne:'8210202331'}
})

note: $ne => this is not equals to operator.

==========Implement  And condition=========

10. WAQ to find users details whose name is saurav and mobile is 8210202331

sql => select * from users where name='saurav' and mobile='8210202331'
mongodb => db.users.find(

  {
    name:'saurav',
    mobile:'8210202331',
  }
)

11. WAQ to find users details whose name is saurav and address is not patna .

sql => select * from users where name ='saurav' and address <>'patna'
mongodb => db.users.find(
  {
    name:'saurav',
    address:{$ne:'patna'}
  }
)
=============greater than operator ========
12. WAQ to find users details whose age is greater then 28.

sql => select * from users where age > 28.
monogod => db.users.find(
   {
    age:{$gt:28}
   }

)
===greater than equals =======
13. WAQ to find student details whose age is 28 or greater than 28 .
sql => select 8 from users where age >=28 ;
mongodb =>db.users.find(
  {
    age:{$gte:28}
  }
)
====== less than equals to =====
13. WAQ to find student details whose age is 28 or less than 28 .
sql => select 8 from users where age >=28 ;
mongodb =>db.users.find(
  {
    age:{$lte:28}
  }
)

======between =======
14. WAQ to find users details whose age is between 28 and 32.

sql=> select * from users where age >28 and age <32 .
mongodb => db.users.find(
  {
    age:{$gt:28 ,$lt:32 }
  }
)
========OR=====================
16 . WAQ to find student details whose age is 22 or 28 or 32 or 34 .

sql=> select * from users where age =22 or age=28 or age=32 or age=34 
      select * from users where age in(22,28,32,34)

mongodb => db.users.find(
        
        {
            $or:[
               {age :22},
               {age :28},
               {age:32},
               {age:34}
            ]

        }

)

17 . WAQ to find users who belongs to delhi or patna 

 sql => select 8 from users where address ='patna' or address='delhi'

 mongodb= db.users.find({
        $or:[
             {address:'patna'},
             {address:'delhi'}
        ]
 });

 
 18. WAQ to find users details department is CS and AI and address is 'patna'

sql => select * from users where department='CS' and department='AI' and address='patna'

momgodb =>  db.users.find(
  {
       $and:[
          {department:'CS'},
          {department:'AI'},
          {adddress:'Patna'}
       ]

  }
)

19.  WAQ to find users details department is CS and address is 'patna ' OR Branch is 'global'

sql=> select  * from users where (department='CS' and address='patna') OR branch='global';

mongodb=> db .users.find({
     $or:[
         {branch:'global'},
         {
            $and:[
              {department :'CS'},
              {address : patna'}
          ]
         }
     ]
})

20. WAQ to find movie whose runtime is greater than 120 minutes or released in year 
    2024.
    Note: colleaction: movies keys: runtime , release_year .

    sql => select * from movies where runtime >120 or  release_year=2024; 
    mongodb =>db.movies.find({
        $or:[
           {runtime:{$gt:120}},
           {release_year:2024}
        ]
    });

21. WAQ to find users whose age in 24, 26 ,28 
sql => select * from users where age in(24,26,28)
mongodb=> db.users.find({
   age:{$in:[24,26,28]}
});
=====in=========
22. WAQ to find users whose age in 24,26,28 and address='patna';

sql => select * from users where age in [24,26,28] and address='patna'

mongodb=> db.users.find({
      
      $and:[
          { age : {$in:[24,26,28]}},
          {address:'patna'}
      ]
})
====not in====
23. WAQ to find users details whose age not in 24,26,28

sql => select * from users where age not in[24,26,28];
mongodb=> db.users.find({
  age:{$nin:[24,26,28]}
})
==== exists======

24. WAQ to find users details whose age not in 24,26,28 
 note: age key should be checked first .

 mongodb=> db.users.find({
        $and:[
            {age:{$exists:true}},
            {age:{$nin:[24,26,28]}}
        ]
        
 })

=====sorting======

25. WAQ to find user details order by age descending.

sql=> select * from users order by age desc;
mongodb=> db.users.find({
      age:{$exists:true}
}).sort(age:-1)

26.WAQ to find products where cateogry='mobile' and brand='samsung' order by price high to low.

sql=> select name, brand, price , availablity  from products where cateogry='mobile' and brand='samsung' order by price desc .

mongodb=> db.products.find({
     $and:[
       {cateogry:'mobile'},
       {brand:'samsung'},
       {price:{$exists:true}}
     ]
}).sort({price:-1});

====limitation======
27: WAQ to find top 5 users.

mongodb=> db.users.find().limit(5)

28. WAQ to find next five users (using skip and limit)

mongodb => db.users.find().skip(5).limit(5)


===sub document query===
29. WAQ to find users details whose permanent address is patna .
mongodb=> db.users.find({
    address.permanent:'patna'
})
=======$all=========
30. WAQ to find users details whose non-acdemic course contain python.
mongodb => db .users.find({
  'course.non_academic':{$all:['python']}
})

31. WAQ to find users details whose non-acdemic course contain java and nodejs
mongodb => db .users.find({
  'course.non_academic':{$all:['java','nodejs']}
})
==== array index match========
32. WAQ to find users details whose non_academic course contain nodejs in second position.
mongodb=> db.users.find({
  'course.non_academic.1':'nodejs'
})
=====$ne====

33. WAQ to find users details whose non_academic course not contain nodejs in second position.

mongodb=> db.users.find({
  'course.non_academic.1':{$ne:'nodejs'}
})

34. WAQ to find users details whose non_academic course not contain nodejs in second position.
    (strictly check course have non_academic course)

mongodb=> db.users.find({
   $and :[
      {'course.non_academic':{$exists:true}},
      {'course.non_academic.1':{$ne:'nodejs'}}
   ]
})

35. WAQ to find users details whose non_academic course contain 'nodejs' and name is 'khusi or swati'

mongodb=>db.users.find({
      $and:[
           {
             'course.non_academic':{$all:['nodejs']}
           },
           {
              $or:[
                {name:'khusi'},
                {name:'Swati'}
              ]
           }

      ]
})


=====================
Aggregation Pipeline
=====================
Link: https://excalidraw.com/#json=68EgKkbwvC-CSmTqivybF,hoSV6dcdBfCgEngSgzWI7w